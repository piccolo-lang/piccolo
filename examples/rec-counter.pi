module Examples.RecCounter

def Count(n:int) =
  [n > 0] tau,
          #core.io:print_int(n), #core.io:print_str("..."),
          Count(n - 1)
+ [true] tau, #core.io:print_str("END\n"), end

def Main() = Count(10)
